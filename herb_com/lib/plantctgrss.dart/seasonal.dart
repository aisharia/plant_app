import 'package:flutter/material.dart';

class Seasonal extends StatelessWidget {
  const Seasonal({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}