import 'package:flutter/material.dart';

class Gardening extends StatelessWidget {
  const Gardening({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}