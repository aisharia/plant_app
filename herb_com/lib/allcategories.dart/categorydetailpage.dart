import 'package:flutter/material.dart';

class CategoryDetailPage extends StatelessWidget {
  const CategoryDetailPage(String title, {super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}